# Running local kernel in QEMU

Local kernel is located in 

```sh
ls /boot/vmlinuz-$(uname -r)
```

Initial RAM disk is located in 

```sh
ls /boot/vmlinuz-$(uname -r)
```

Kernel config is located in 

```sh
ls /boot/config-$(uname -r)
```
